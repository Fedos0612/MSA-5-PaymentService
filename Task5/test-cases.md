# Task 5. Таблица тест-кейсов для OrchestrPay

| Название | Тип | Компоненты | Предусловия |
|---|---|---|---|
| Успешная обработка платежа при решении антифрода `ALLOW` | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `FraudCheck Service`, `Notification Service`, Zeebe, Operate | Camunda stack запущен, BPMN-процесс задеплоен, Java workers активны, сценарий запускается с параметром `scenario=allow` |
| Отказ антифрода и автоматический возврат клиенту | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `FraudCheck Service`, `Notification Service`, `Security System`, Zeebe | Camunda stack запущен, BPMN-процесс задеплоен, Java workers активны, сценарий запускается с параметром `scenario=deny` |
| Ручная антифрод-проверка с подтверждением операции | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `FraudCheck Service`, Tasklist, Zeebe | Camunda stack запущен, BPMN-процесс задеплоен, пользовательская задача доступна в Tasklist, сценарий запускается с параметром `scenario=manual-allow` |
| Ручная антифрод-проверка с отказом операции | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `FraudCheck Service`, Tasklist, `Security System`, Zeebe | Camunda stack запущен, BPMN-процесс задеплоен, пользовательская задача доступна в Tasklist, сценарий запускается с параметром `scenario=manual-deny` |
| Срабатывание cut-off-time при отсутствии решения ручной проверки | End-to-end | `Payment Saga Orchestrator`, Tasklist, Zeebe, `Payment Service` | Camunda stack запущен, BPMN-процесс задеплоен, запущен сценарий `scenario=manual-cutoff-fast`, пользовательская задача не завершается вручную |
| Сбой списания средств со счёта клиента | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `Notification Service`, Zeebe | Camunda stack запущен, BPMN-процесс задеплоен, Java workers активны, сценарий запускается с параметром `scenario=debit-fail` |
| Сбой перевода средств контрагенту и запуск компенсационного возврата | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, `Notification Service`, Zeebe | Camunda stack запущен, BPMN-процесс задеплоен, списание прошло успешно, сценарий запускается с параметром `scenario=transfer-fail` |
| Проверка создания экземпляра процесса через REST API сервиса | Интеграционный | REST API `payment-service`, Zeebe Client, Zeebe Gateway | `payment-service` запущен, Zeebe доступен по адресу `zeebe:26500`, BPMN-процесс задеплоен |
| Проверка worker-а `create-payment` | Интеграционный | `payment-service`, Zeebe worker `create-payment` | В Zeebe создан job типа `create-payment`, worker запущен и подписан на этот job type |
| Проверка worker-а `debit-customer` при успешном списании | Интеграционный | `payment-service`, Zeebe worker `debit-customer` | В переменных процесса передан сценарий, не равный `debit-fail`; job типа `debit-customer` доступен |
| Проверка worker-а `debit-customer` при ошибке списания | Интеграционный | `payment-service`, Zeebe worker `debit-customer` | В переменных процесса передан сценарий `debit-fail`; job типа `debit-customer` доступен |
| Проверка worker-а `fraud-check` для результата `ALLOW` | Интеграционный | `payment-service`, Zeebe worker `fraud-check` | В переменных процесса передан сценарий `allow`; job типа `fraud-check` доступен |
| Проверка worker-а `fraud-check` для результата `DENY` | Интеграционный | `payment-service`, Zeebe worker `fraud-check` | В переменных процесса передан сценарий `deny`; job типа `fraud-check` доступен |
| Проверка worker-а `fraud-check` для результата `MANUAL_REVIEW` | Интеграционный | `payment-service`, Zeebe worker `fraud-check`, Tasklist | В переменных процесса передан сценарий `manual-allow` или `manual-deny`; job типа `fraud-check` доступен |
| Проверка worker-а `apply-cutoff-allow` | Интеграционный | `Payment Saga Orchestrator`, Zeebe timer boundary event, worker `apply-cutoff-allow` | Запущен сценарий `manual-cutoff-fast`, user task не завершена вручную, истёк timer boundary event |
| Проверка worker-а `transfer-to-counterparty` при успешном переводе | Интеграционный | `payment-service`, Zeebe worker `transfer-to-counterparty` | Процесс дошёл до pivot point, сценарий не равен `transfer-fail` |
| Проверка worker-а `transfer-to-counterparty` при сбое перевода | Интеграционный | `payment-service`, Zeebe worker `transfer-to-counterparty` | Процесс дошёл до pivot point, в переменных процесса передан сценарий `transfer-fail` |
| Проверка worker-а `refund-customer` после отказа антифрода | Интеграционный | `payment-service`, Zeebe worker `refund-customer` | Антифрод вернул `DENY`, процесс перешёл в ветку блокировки и компенсации |
| Проверка worker-а `refund-customer` после сбоя перевода контрагенту | Интеграционный | `payment-service`, Zeebe worker `refund-customer` | Списание прошло успешно, перевод контрагенту завершился с результатом `FAILED` |
| Проверка отправки уведомления клиенту при успешном платеже | Интеграционный | `payment-service`, Zeebe worker `notify-success`, `Notification Service` | Процесс успешно выполнил `TRANSFER_TO_COUNTERPARTY` |
| Проверка отправки уведомления клиенту при отклонении платежа | Интеграционный | `payment-service`, Zeebe worker `notify-decline`, `Notification Service` | Платёж завершился отказом антифрода, ошибкой списания или компенсационным возвратом |
| Проверка отправки уведомления в систему безопасности | Интеграционный | `payment-service`, Zeebe worker `send-security-alert`, `Security System` | Антифрод вернул `DENY` или ручная проверка завершилась решением `DENY` |
| Проверка параллельной ветки компенсации при подозрительной операции | End-to-end | `Payment Saga Orchestrator`, `Payment Service`, `Notification Service`, `Security System`, Zeebe | Запущен сценарий `deny`; после блокировки должны выполниться возврат клиенту и уведомление безопасности |
| Проверка отсутствия «зависания» денег после отказа антифрода | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, Zeebe, Operate | Списание прошло успешно, антифрод вернул `DENY`, должен быть выполнен автоматический возврат |
| Проверка отсутствия «зависания» денег после сбоя перевода контрагенту | End-to-end | `Payment Service`, `Payment Saga Orchestrator`, Zeebe, Operate | Списание прошло успешно, перевод контрагенту завершился ошибкой, должен быть выполнен автоматический возврат |
| Проверка корректного завершения процесса без incident в Operate | End-to-end | Zeebe, Operate, `payment-service` | Запущен любой валидный сценарий: `allow`, `deny`, `manual-allow`, `manual-deny`, `manual-cutoff-fast` или `transfer-fail` |
| Проверка отображения ручной задачи в Tasklist | End-to-end | Tasklist, Zeebe, `FraudCheck Service`, `Payment Saga Orchestrator` | Запущен сценарий `manual-allow` или `manual-deny`, антифрод вернул `MANUAL_REVIEW` |
| Проверка завершения ручной задачи через Tasklist | End-to-end | Tasklist, Zeebe, `Payment Saga Orchestrator`, `Payment Service` | В Tasklist есть задача `Manual antifraud review`, пользователь завершает её через UI |
| Проверка повторного запуска нескольких платежей подряд | End-to-end | REST API `payment-service`, Zeebe, Operate, workers | Camunda stack запущен, BPMN-процесс задеплоен, последовательно запускаются несколько process instances с разными сценариями |
| Проверка идемпотентности бизнес-операций по `paymentId` и `sagaId` | Интеграционный | `Payment Service`, `Payment Saga Orchestrator`, Zeebe | Для одного платежа повторно вызывается обработчик или повторно активируется job; операции не должны приводить к двойному списанию, двойному возврату или двойному переводу |
| Проверка обработки недоступности Zeebe при старте платежа | Интеграционный | REST API `payment-service`, Zeebe Client, Zeebe Gateway | `payment-service` запущен, Zeebe временно недоступен или BPMN-процесс не задеплоен |
| Проверка некорректного значения `scenario` | Интеграционный | REST API `payment-service`, `fraud-check` worker, Zeebe | Запрос `/payments/start` отправлен с неизвестным значением `scenario`; сервис должен обработать значение по дефолтному безопасному сценарию или вернуть понятную ошибку |
| Проверка доступности health endpoint сервиса | Интеграционный | REST API `payment-service` | Контейнер `payment-service` запущен |
| Проверка деплоя BPMN-процесса через `deploy-process` | Интеграционный | `deploy-process`, Zeebe Gateway, `process.bpmn` | Zeebe запущен и готов принимать команды, файл `process.bpmn` доступен в volume |